# Generated by Cargo
# Note: Cargo.lock IS committed. This crate is a binary application,
# not a library, so the lockfile guarantees reproducible builds across
# machines and CI. Only the build output (target/) is excluded.
target/

# Generated by Tauri
gen/
