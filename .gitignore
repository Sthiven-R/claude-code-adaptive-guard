# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.pytest_cache/

# Telemetry
telemetry/*.log
telemetry/*.jsonl

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Local overrides
config/local.json
.env

# wrangler files (Cloudflare Workers config — added by the
# cloudflare-workers-and-pages bot via PR #1)
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

# Local-built portable binary (built via `npm run tauri build --no-bundle`).
# Not versioned — release artifacts ship through the GitHub release page.
/Adaptive Guard.exe
